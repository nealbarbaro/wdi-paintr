%h1 Paintr

#selected_color.box
/ create div with selected color id w/ class box

#color_form
  = text_field_tag 'color', nil, :autofocus => true
  = button_tag 'Add Color', :id => 'add_color'

#colors

#canvas
  - 1000.times do
    .paint_box
